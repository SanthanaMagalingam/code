#include <stdio.h>
int main()
{
    int n,s,t=1;
    scanf("%d",&n);
    for(int i=1;i<=n;i++)
    {
        s=t;
        for(int j=1;j<=n;j++)
        {
            printf("* ");
        }
        printf("\n");
        for(int k=1;k<=i;k++)
        {
            printf("  ");
        }
    }
}
